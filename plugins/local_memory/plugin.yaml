name: local_memory
version: 1.2.0
description: "Local sidecar memory provider with upgrade-safe compatibility adapter (read-only Hermes SQLite + writable sidecar index)."
requires_env: []
pip_dependencies:
  - "hindsight-client>=0.5.0"
hooks:
  - on_session_end
